<%= form_for([@recipe, @new_ingredient]) do |f| %>

  <% if @new_ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

      <ul>
      <% @new_ingredient.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>  <br />

    <%= f.label :quantity %>  <br />
    <%= f.text_field :quantity %>   <br />

    <%= f.label :um %>  <br />
    <%= f.text_field :um %>   <br />

    <%= f.label :allow_scale %>   <br />
    <%= f.check_box :allow_scale %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
