<%= form_for @category,  :html => { :multipart => true } do |f| %>

<div class="field">
  <%= f.file_field :image %>

  <%= f.label :description %><br />
  <%= f.text_field :description %>
</div>
<div class="actions">
    <%= f.submit %>
</div>
<%end%>